server {
    listen 80;
    server_name music.gaiasentinel.online;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name music.gaiasentinel.online;

    # SSL — managed by certbot / Cloudflare origin cert
    ssl_certificate     /etc/letsencrypt/live/music.gaiasentinel.online/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/music.gaiasentinel.online/privkey.pem;

    location / {
        proxy_pass         http://127.0.0.1:5050;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}
